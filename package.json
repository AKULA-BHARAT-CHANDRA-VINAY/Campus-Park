{
    "name": "smart-parking-system",
    "version": "1.0.0",
    "description": "Unified Smart Parking System",
    "scripts": {
        "install-all": "npm install && cd frontend && npm install && cd ../admin && npm install && cd ../backend && npm install",
        "build": "cd frontend && npm run build && cd ../admin && npm run build",
        "start": "cd backend && npm start",
        "dev": "concurrently \"cd frontend && npm run dev\" \"cd admin && npm start\" \"cd backend && npm start\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}